import { CustomDeractiveTestDirective } from './custom-deractive-test.directive';

describe('CustomDeractiveTestDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDeractiveTestDirective();
    expect(directive).toBeTruthy();
  });
});
